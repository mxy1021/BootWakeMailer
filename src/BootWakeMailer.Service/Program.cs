namespace BootWakeMailer.Service;

/// <summary>
/// Entry point of the Windows Service host.
/// </summary>
/// <remarks>
/// Placeholder only. Startup event capture, power-event handling, queue processing,
/// the 60-second retry timer and the immediate-retry custom command belong to the
/// service phase (architecture.md §3.2) and are deliberately not implemented here.
/// </remarks>
internal static class Program
{
    private static void Main()
    {
    }
}
