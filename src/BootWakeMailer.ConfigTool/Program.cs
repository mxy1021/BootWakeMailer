namespace BootWakeMailer.ConfigTool;

/// <summary>
/// Entry point of the local WinForms configuration tool.
/// </summary>
/// <remarks>
/// Placeholder only. The configuration form, test mail, service-state display and
/// retry command belong to the configuration-tool phase (architecture.md §3.3) and
/// are deliberately not implemented here.
/// </remarks>
internal static class Program
{
    [STAThread]
    private static void Main()
    {
    }
}
